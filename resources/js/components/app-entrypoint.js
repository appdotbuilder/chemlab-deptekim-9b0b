// Laravel Livewire Application Entry Point
// This file replaces the React/Inertia setup with Livewire + Alpine.js

// Alpine.js setup would go here when dependencies are installed:
// import Alpine from 'alpinejs';
// import { animate, inView } from 'motion';
// import flatpickr from 'flatpickr';

// For now, we create a basic setup that works with existing structure
console.log('Livewire app entry point loaded');

// When proper packages are installed, uncomment this:
/*
window.Alpine = Alpine;
Alpine.start();

// Hero section animation
window.addEventListener('DOMContentLoaded', () => {
  const hero = document.querySelector('[data-hero]');
  if (hero && window.animate) {
    window.animate(hero, { opacity: [0, 1], y: [-12, 0] }, { duration: 0.6, easing: 'ease-out' });
  }
});

// Expose utilities globally
window.flatpickr = flatpickr;
*/